/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { SELLO_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REVOKE_CONSENT_DISCRIMINATOR = new Uint8Array([
  36, 0, 100, 148, 132, 131, 112, 76,
]);

export function getRevokeConsentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVOKE_CONSENT_DISCRIMINATOR,
  );
}

export type RevokeConsentInstruction<
  TProgram extends string = typeof SELLO_PROGRAM_ADDRESS,
  TAccountSello extends string | AccountMeta<string> = string,
  TAccountVoiceConsent extends string | AccountMeta<string> = string,
  TAccountAuthor extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSello extends string
        ? WritableAccount<TAccountSello>
        : TAccountSello,
      TAccountVoiceConsent extends string
        ? WritableAccount<TAccountVoiceConsent>
        : TAccountVoiceConsent,
      TAccountAuthor extends string
        ? WritableSignerAccount<TAccountAuthor> &
            AccountSignerMeta<TAccountAuthor>
        : TAccountAuthor,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeConsentInstructionData = {
  discriminator: ReadonlyUint8Array;
  target: number;
};

export type RevokeConsentInstructionDataArgs = { target: number };

export function getRevokeConsentInstructionDataEncoder(): FixedSizeEncoder<RevokeConsentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["target", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REVOKE_CONSENT_DISCRIMINATOR }),
  );
}

export function getRevokeConsentInstructionDataDecoder(): FixedSizeDecoder<RevokeConsentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["target", getU8Decoder()],
  ]);
}

export function getRevokeConsentInstructionDataCodec(): FixedSizeCodec<
  RevokeConsentInstructionDataArgs,
  RevokeConsentInstructionData
> {
  return combineCodec(
    getRevokeConsentInstructionDataEncoder(),
    getRevokeConsentInstructionDataDecoder(),
  );
}

export type RevokeConsentInput<
  TAccountSello extends string = string,
  TAccountVoiceConsent extends string = string,
  TAccountAuthor extends string = string,
> = {
  sello: Address<TAccountSello>;
  voiceConsent: Address<TAccountVoiceConsent>;
  author: TransactionSigner<TAccountAuthor>;
  target: RevokeConsentInstructionDataArgs["target"];
};

export function getRevokeConsentInstruction<
  TAccountSello extends string,
  TAccountVoiceConsent extends string,
  TAccountAuthor extends string,
  TProgramAddress extends Address = typeof SELLO_PROGRAM_ADDRESS,
>(
  input: RevokeConsentInput<
    TAccountSello,
    TAccountVoiceConsent,
    TAccountAuthor
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeConsentInstruction<
  TProgramAddress,
  TAccountSello,
  TAccountVoiceConsent,
  TAccountAuthor
> {
  // Program address.
  const programAddress = config?.programAddress ?? SELLO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    sello: { value: input.sello ?? null, isWritable: true },
    voiceConsent: { value: input.voiceConsent ?? null, isWritable: true },
    author: { value: input.author ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.sello),
      getAccountMeta(accounts.voiceConsent),
      getAccountMeta(accounts.author),
    ],
    data: getRevokeConsentInstructionDataEncoder().encode(
      args as RevokeConsentInstructionDataArgs,
    ),
    programAddress,
  } as RevokeConsentInstruction<
    TProgramAddress,
    TAccountSello,
    TAccountVoiceConsent,
    TAccountAuthor
  >);
}

export type ParsedRevokeConsentInstruction<
  TProgram extends string = typeof SELLO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    sello: TAccountMetas[0];
    voiceConsent: TAccountMetas[1];
    author: TAccountMetas[2];
  };
  data: RevokeConsentInstructionData;
};

export function parseRevokeConsentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeConsentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      sello: getNextAccount(),
      voiceConsent: getNextAccount(),
      author: getNextAccount(),
    },
    data: getRevokeConsentInstructionDataDecoder().decode(instruction.data),
  };
}
